#Excel

---
### 검색 함수
==VLOOKUP(찾을 값, 범위, 열 인덱스 번호)==
찾을 값을 범위 내에서 세로로(Vertical) 찾고, 그 찾은 지점에서 인덱스 번호(1부터 시작)만큼 떨어진 열에 해당하는 값을 반환

==HLOOKUP(찾을 값, 범위, 행 인덱스 번호)==
찾을 값을 범위 내에서 가로로(Horizontal) 찾고, 그 찾은 지점에서 인덱스 번호(1부터 시작)만큼 떨어진 행에 해당하는 값을 반환

!! 중요 !!
찾을 값은 해당 범위의 첫 번째 열에 있어야 함
