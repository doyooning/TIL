
---
# *Codex의 답변 메모*

# 구매 및 결제 부분 동시성 처리 관련

현재 구현을 기준으로 보면, 구매/결제 동시성 처리는 이미 일부 강하게 들어가 있고, Redisson은 “부분 적용”이 맞습니다.

  현재 구현 로직 (핵심)

  1. 구매(주문 생성) createOrder

  - 상품별로 PESSIMISTIC_WRITE로 행 잠금 후 재고 차감합니다.
  - 경로: OrderService.java:53, OrderService.java:91, ProductRepository.java:54, Product.java:104
  - productIds 정렬 후 잠금 획득해서 데드락 가능성도 줄여둔 상태입니다. OrderService.java:87

  2. 결제 승인 confirmPayment

  - 주문 행을 FOR UPDATE 성격으로 잠그고 처리합니다.
  - 결제사 호출에 Idempotency-Key를 넣습니다.
  - 경로: TossPaymentService.java:74, TossPaymentService.java:89, OrderRepository.java:41, TossPaymentService.java:121

  3. 취소/환불
  
  - 상태 조건부 UPDATE ... WHERE status=...로 CAS 패턴으로 처리합니다.
  - 동시 취소 통합테스트도 존재합니다.
  - 경로: OrderService.java:235, OrderRepository.java:60, OrderRepository.java:77, OrderCancelConcurrencyIntegrationTest.java:47

  Redisson 적용 적합성 판단

  - 재고 차감(createOrder)에는 필수 아님: DB 비관적 락이 이미 정합성의 기준이며, Redisson을 추가해도 정확성이 크게 좋아지지 않고 복잡도/장애면만 늘어날 수 있습니다.
  - 결제 승인/취소에는 적합: 외부 결제사 API 호출이 포함되어 중복 호출 방지(특히 동일 주문 동시 요청) 목적의 분산락이 유효합니다.
  - 결론: “전체 구매/결제에 일괄 적용”보다, 결제 confirm/cancel 경로에 주문 단위 Redisson 락을 거는 게 맞습니다.

  추가로 보이는 리스크

  - toss_payment는 toss_payment_key만 유니크이고 order_id는 유니크가 아닙니다. TossPayment.java:27, livecommerce_create_table.sql:609
  - 그래서 실무적으로는 lock:order:payment:{orderId} + DB 상태검증을 같이 두는 구성이 안전합니다.


구매/결제 동시성은 Redisson이 아니라 DB 락 + 조건부 업데이트로 처리되어 있습니다.

  1. 주문 생성(구매) 시 재고 동시성

  - OrderService.createOrder()에서 상품별로 findByIdForUpdateAndStatus(...) 조회를 사용합니다.
    OrderService.java:91
  - 이 메서드는 PESSIMISTIC_WRITE 락 쿼리입니다.
    ProductRepository.java:52
  - 락 잡힌 상태에서 재고 검증 후 decreaseStock()로 차감하고, 주문/주문아이템 저장까지 한 트랜잭션으로 진행됩니다.
    OrderService.java:40, OrderService.java:99, OrderService.java:127

  1. 결제 승인(confirm) 시 동시성

  - confirmPayment() 시작 시 주문을 findOrderForUpdate()로 조회하고, 내부에서 findByIdForUpdate / findByOrderNumberForUpdate를 사용해 주문 행에 PESSIMISTIC_WRITE를 겁니다.
    TossPaymentService.java:89, TossPaymentService.java:275, OrderRepository.java:39
  - 금액/가격 재검증 후, 동일 paymentKey 기존 결제건 있으면 재사용(멱등 처리)합니다.
    TossPaymentService.java:101
  - Toss API 호출에도 Idempotency-Key를 넣습니다.
    TossPaymentService.java:115, TossPaymentService.java:121
  - DB에는 toss_payment_key 유니크 제약이 있어 중복 저장을 막습니다.
    TossPayment.java:27

  1. 취소/환불 시 동시성

  - 상태 전이는 where status = ... 조건부 update로 원자 처리합니다 (CREATED -> CANCELLED, PAID -> REFUND_REQUESTED, REFUND_REQUESTED -> REFUNDED).
    OrderRepository.java:49, OrderRepository.java:67, OrderRepository.java:83
  - 서비스에서 updated == 0이면 상태 경합으로 보고 409 처리합니다.
    OrderService.java:264, OrderService.java:273

  1. Redisson 적용 여부

  - 프로젝트에 Redisson 의존성은 있습니다.
    build.gradle:50
  - 하지만 현재 구매/결제(order/payment) 코드 경로에서는 Redisson 락을 사용하지 않습니다.
  - Redisson 락 사용 코드는 라이브호스트 Redis 서비스 쪽(acquireLock/releaseLock)입니다.
    RedisService.java:120, RedisService.java:134



# 방송 예약 경합 처리 및 Redisson 활용

Redisson의 장점

• Redisson의 장점은 "Redis를 분산 락 용도로 쓸 때 필요한 기능을 직접 구현하지 않아도 꽤 안전하게 제공한다"는 점입니다.

  다른 방식과 비교하면 이렇게 볼 수 있습니다.

  1. setIfAbsent 같은 단순 Redis 락보다 나은 점

  - 락 획득 대기 시간과 점유 시간을 tryLock(waitTime, leaseTime)으로 명확하게 제어할 수 있습니다.
  - 락 해제 시 현재 스레드가 가진 락인지 확인하고 해제할 수 있어, 남의 락을 잘못 푸는 실수를 줄입니다.
  - 재진입 락, 공정 락 같은 고급 락 기능을 바로 쓸 수 있습니다.
  - watchdog 기반 자동 연장 기능이 있어, 작업이 길어져도 락이 중간에 예상치 않게 풀릴 위험을 줄일 수 있습니다.

  1. DB 락만 사용하는 방식보다 나은 점

  - DB에 직접 락 부담을 몰지 않고, 빠른 Redis 레벨에서 먼저 경쟁을 줄일 수 있습니다.
  - 락 획득/해제 비용이 보통 DB보다 가볍습니다.
  - 애플리케이션 여러 인스턴스가 떠 있어도 분산 환경에서 공통 락으로 쓰기 쉽습니다.

  1. synchronized나 JVM 내부 락보다 나은 점

  - synchronized는 한 서버 프로세스 안에서만 유효합니다.
  - 서버가 여러 대면 JVM 락으로는 동시성 제어가 안 됩니다.
  - Redisson은 Redis를 기준으로 잡기 때문에 멀티 인스턴스 환경에서도 같은 기준으로 락을 공유할 수 있습니다.

  1. 직접 구현한 분산 락보다 나은 점

  - 락 키 만료, 재시도, 해제 안정성 같은 까다로운 부분을 직접 구현하지 않아도 됩니다.
  - 운영 중 발생하기 쉬운 "락 유실", "잘못된 해제", "타임아웃 처리 누락" 같은 실수를 줄일 수 있습니다.
  - 코드가 더 짧고 의도가 명확해집니다.

  실무적으로 요약하면:

  - synchronized: 단일 서버용
  - DB 락: 강하지만 무겁고 DB 의존적
  - setIfAbsent 직접 구현: 단순하지만 실수하기 쉬움
  - Redisson: 분산 환경에서 쓰기 편하고, 락 기능이 풍부하고, 운영 안정성이 상대적으로 좋음

  다만 Redisson도 만능은 아닙니다.

  - Redis 자체 가용성에 영향을 받습니다.
  - 비즈니스 정합성의 최종 보장은 여전히 DB 검증이 필요할 수 있습니다.
  - 그래서 지금 구조처럼 "Redisson으로 1차 제어 + DB 검증/보강" 조합이 실용적입니다.   


# 인증/인가 구조

이 프로젝트 인증/인가는 OAuth2 로그인 + JWT(access/refresh) + Redis refresh 저장 + 경로 기반 권한 체크 구조입니다.

  1. 로그인/토큰 발급

  - 소셜 로그인 진입: 프론트에서 /oauth2/authorization/{provider}로 이동합니다.
    Login.vue:12
  - OAuth2 성공 시 서버가 역할별로 처리:
      - 신규 유저(ROLE_GUEST): 5분짜리 signup 토큰 발급 후 /signup으로 리다이렉트
      - 관리자(ROLE_ADMIN): 바로 JWT 발급하지 않고 관리자 2차 인증 세션 시작 후 /admin/verify로 이동
      - 일반/판매자: access(10분), refresh(24시간) JWT 발급 + refresh Redis 저장
        CustomSuccessHandler.java:38

  1. JWT 검증/인증 주입

  - 모든 API 요청에서 JWTFilter가 토큰을 읽습니다(우선순위: Authorization: Bearer → access 헤더 → access 쿠키).
  - 토큰 만료/카테고리(access)를 검증하고, 유효하면 SecurityContext에 Authentication을 넣습니다.
    JWTFilter.java:29
  - JWT claim은 username, role, category 기반입니다.
    JWTUtil.java:60

  1. 재발급/로그아웃

  - 재발급: /api/reissue가 refresh 쿠키를 검증 후 새 access/refresh 발급, 기존 refresh는 Redis에서 삭제 후 새로 저장합니다.
    ReissueController.java:27
  - 로그아웃: /logout POST에서 refresh 유효성 확인 후 Redis refresh 삭제 + access/refresh 쿠키 제거.
    CustomLogoutFilter.java:39                                                                                                                                                                                                      
  - refresh 토큰 저장소는 Redis(refresh:{token} 키)입니다.                                                                                                                                                                          
    RefreshRepository.java:10

  1. 인가(Authorization)

  - SecurityConfig에서 URL 패턴별 hasAuthority/hasAnyAuthority로 권한 제어:
      - 예: /api/orders/**는 ROLE_MEMBER, /api/admin/**는 ROLE_ADMIN
      - 일부 공개 API는 permitAll
        SecurityConfig.java:173
  - /api/my는 인증된 CustomOAuth2User가 있어야 응답합니다.
    MyPageController.java:21

  1. 관리자 2차 인증(추가 인가 레이어)

  - 관리자 로그인 후 세션에 pending 상태를 두고, /api/admin/auth/verify 성공 전에는 admin API 접근을 막습니다.
    AdminSecondFactorFilter.java:53
    AdminAuthController.java:22

  1. 프론트 인증 동작
  - Axios는 withCredentials: true + Authorization 자동 주입.
  - 초기 진입 시 hydrateSessionUser()가 /api/my 호출, 401이면 /api/reissue 후 재시도.
  - 라우터 가드에서 /seller, /admin 경로를 역할별로 차단합니다.
    http.ts:13
    auth.ts:222
    router/index.ts:308


# 토큰 재발급

 1) 토큰 만료 처리

  - Access 토큰:
      - JWTFilter가 요청에서 토큰 추출 후 만료 검사(jwtUtil.isExpired)를 합니다.
      - 만료/유효하지 않으면 401 반환합니다.
      - 참고: JWTFilter.java:32, JWTFilter.java:43, JWTFilter.java:49, JWTFilter.java:56
  - 만료 판정 로직:
      - exp 클레임과 현재 시각 비교(before(new Date()))로 판단합니다.
      - 참고: JWTUtil.java:55, JWTUtil.java:57

  1) 재발급(/api/reissue)

  - 엔드포인트: ReissueController.java:27                                                                                                                                                                                           
  - 흐름:
      1. 쿠키에서 refresh 추출
      2. 없으면 400 ("refresh token null")
      3. 만료면 400 ("refresh token expired")
      4. category != refresh면 400 ("invalid refresh token")
      5. 새 Access(10분), 새 Refresh(24시간) 발급
      6. Redis에서 기존 refresh 삭제 후 새 refresh 저장(TTL 포함)
      7. 응답 헤더 access + access/refresh 쿠키 반환
  - 참고:
    ReissueController.java:46, ReissueController.java:51, ReissueController.java:66, ReissueController.java:73, ReissueController.java:74, ReissueController.java:79, ReissueController.java:80, ReissueController.java:83,         
    ReissueController.java:84, ReissueController.java:85

  1) 최초 발급(로그인 성공 시)

  - Access 10분, Refresh 24시간으로 생성하고 Refresh는 Redis에 저장합니다.
  - 참고: CustomSuccessHandler.java:79, CustomSuccessHandler.java:80, CustomSuccessHandler.java:84

  1) Redis 저장 방식

  - key: refresh:{토큰}, value: username, TTL: refresh 만료시간(ms)
  - 참고: RefreshRepository.java:19, RefreshRepository.java:23, RefreshRepository.java:28


# 결제 기능

이 프로젝트 결제 설계는 주문 선생성 → 토스 결제창 → 서버 확정(confirm) → 주문 PAID 전이 구조입니다.

  1. 백엔드 핵심 구조

  - 주문 생성/조회/취소 API: OrderController.java:29
  - 토스 결제 확정 API: TossPaymentController.java:15
  - 실제 결제/환불 연동 로직: TossPaymentService.java:40
  - 주문 상태 전이/취소 처리: OrderService.java:42, Order.java:30

  1. 결제 시퀀스

  - 프론트가 먼저 POST /api/orders로 주문을 생성합니다. (상태 CREATED)
  - 이후 토스 위젯 결제를 띄우고, 성공 콜백에서 paymentKey/orderId/amount로 POST /api/payments/toss/confirm 호출합니다.
      - 프론트 호출: Checkout.vue:542, PaymentSuccess.vue:70
      - API 클라이언트: payments.ts:13
  - 서버는 confirm 시:
      - 주문 행 잠금 조회 (findByIdForUpdate/findByOrderNumberForUpdate)
      - 금액 검증 및 현재가 재검증
      - 토스 /v1/payments/confirm 호출
      - toss_payment 저장 + 주문 PAID 전이
      - 근거: TossPaymentService.java:74

  2. 취소/환불 설계

  - 사용자 취소 API: /api/orders/{id}/cancel
  - CREATED 주문 취소: DB 업데이트로 바로 CANCELLED 처리
  - PAID 주문 취소: REFUND_REQUESTED로 바꾼 뒤 토스 취소 API 호출, 성공 시 REFUNDED
  - 근거: OrderService.java:235, OrderRepository.java:60, TossPaymentService.java:154

  2. 데이터 모델

  - 주문 상태 enum: CREATED, PAID, CANCELLED, REFUND_REQUESTED, REFUNDED ...
      - OrderStatus.java:3
  - 결제/환불 영속화:
      - toss_payment: TossPayment.java:20
      - toss_refund: TossRefund.java:19
  - 스키마: livecommerce_create_table.sql:306, livecommerce_create_table.sql:597

  2. 특징/안전장치

  - 결제 confirm/cancel 둘 다 idempotency key 생성 후 토스 호출
  - 프론트에서 15초 주기로 가격/재고 재동기화 후 결제 진행
      - Checkout.vue:128, Checkout.vue:671
  - 결제 실패/예외 시 생성 주문 정리용 /abandon 호출
      - PaymentFail.vue:19, OrderService.java:207
  - 권한은 /api/payments/toss/** 가 ROLE_MEMBER
      - SecurityConfig.java:191 

#  토큰 발급/재발급

  변경 내용

  1. 블랙리스트 저장소 추가
     AccessBlacklistRepository.java

  - Redis 키: blacklist:access:< token >
  - blacklist(token, ttlMs), isBlacklisted(token) 구현

  1. JWT 만료 잔여시간 계산 추가
     JWTUtil.java:60

  - getRemainingMs(token) 추가 (블랙리스트 TTL 계산용)

  1. 인증 필터에서 블랙리스트 차단                                                                                                                                                                                                 
     JWTFilter.java:45                                                                                                                                                                                                             

  - 토큰 추출 후 isBlacklisted면 즉시 401

  1. 로그아웃 시 access 블랙리스트 등록                                                                                                                                                                                            
     CustomLogoutFilter.java:58                                                                                                                                                                                                    

  - POST /logout 요청에서 access 토큰(Authorization/access 헤더/쿠키) 추출
  - 유효한 access 토큰이면 남은 TTL로 Redis 블랙리스트 등록
  - 기존 refresh 삭제/쿠키 만료 로직은 유지

  1. 탈퇴(quit) 정리 로직에도 동일 적용
     TokenCleanupService.java:25

  - 토큰 정리 시 access 블랙리스트 등록 추가

  1. 보안 설정 DI 연결
     SecurityConfig.java:145

  - JWTFilter, CustomLogoutFilter에 AccessBlacklistRepository 주입