#프로젝트

---
# 주요 문제 해결
### **JPA N+1 문제**
요약하면, 이 프로젝트는 상품 목록 조회에서 연관 엔티티를 개별로 지연로딩하지 않고, 
필요한 데이터만 “배치 조회 + DTO 조합”으로 N+1을 피합니다. 
그리고 전역적으로 Hibernate batch fetch size도 설정되어 있습니다.

핵심 대응 방식 (상품 조회 기준)

  1. 상품 목록 조회 시 태그를 한 번에 배치 조회
     `ProductService.getProducts()`에서 상품 목록을 먼저 가져온 다음,  상품 ID 리스트로 태그를 IN 쿼리로 한 번에 조회합니다.                                                                                                           
     이때 `ProductTagRepository.findActiveTagsByProductIds(...)`는 projection 쿼리로 필요한 컬럼만 조회해 DTO로 조합합니다.                                                                                                           
     파일: `src/main/java/com/deskit/deskit/product/service/ProductService.java`
     파일: `src/main/java/com/deskit/deskit/product/repository/ProductTagRepository.java`                                                                                                                                            
  2. 썸네일/이미지, 라이브 가격도 배치 조회
     같은 `getProducts()`에서 이미지와 라이브 가격도 productIds로 한 번에 조회하고, 결과를 맵으로 묶어 응답 DTO에 조합합니다.
	  
  3. Hibernate batch fetch size 설정
     전역적으로 `hibernate.default_batch_fetch_size=100`이 설정되어 있어, LAZY 연관 로딩이 발생하더라도 묶음으로 가져오는 힌트가 제공됩니다.
     파일: `src/main/resources/application.properties`


아래는 상품 목록 조회(getProducts) 기준으로 실제 나가는 쿼리 흐름을 단계별로 정리한 것입니다. 
단건 조회(getProduct)는 별도 섹션에 덧붙입니다.

###### 상품 목록 조회 흐름 (ProductService.getProducts)

  1. 상품 기본 목록 조회
      - 목적: 삭제되지 않았고 판매중인 상품만 가져옴
      - 쿼리 형태:
```sql
		select *
		from Product
		where status = 'ON_SALE'
		order by id asc
```
        
      - 파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
      
  2. 썸네일 이미지 일괄 조회
      - 목적: 상품별 썸네일(슬롯 0)만 한 번에 조회
      - 쿼리 형태:
```sql
        select *
        from ProductImage
        where product_id in ( ...productIds... )
            and image_type = 'THUMBNAIL'
            and slot_index = 0
        order by product_id asc, id asc
```
		
      - 파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
      
  3. 라이브 가격 일괄 조회
      - 목적: 방송 중 상품의 현재 가격 오버라이드 조회
      - 쿼리 형태(개념적으로):
```sql
        select product_id, bp_price
        from BroadcastProduct
        and (라이브 상태 조건...)
```
		
      - 파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
    
  4. 상품 태그 일괄 조회 (N+1 방지 핵심)
      - 목적: 상품별 태그를 한 번에 가져와 DTO로 조립
      - 쿼리 형태:
```sql
	    select pt.product.id as productId,
               tc.tagCode as tagCode,
               t.tagName as tagName
        from ProductTag pt
        join pt.tag t
        join t.tagCategory tc
        where pt.deletedAt is null
           and t.deletedAt is null
           and tc.deletedAt is null
           and pt.product.id in ( ...productIds... )
        order by pt.product.id, tc.tagCode, t.tagName
```

  5. 메모리에서 DTO 조합
      - 쿼리 없음 (메모리 처리)
  
###### 상품 단건 조회 흐름 (ProductService.getProduct)

  1. 상품 단건 조회
```sql
     select *
       from Product
        and status = 'ON_SALE'
        and deleted_at is null
```
	
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java

  2. 태그 일괄 조회 (단건도 동일 로직 재사용)
```sql
     select pt.product.id as productId,
            tc.tagCode as tagCode,
            t.tagName as tagName
       from ProductTag pt
       join pt.tag t
       join t.tagCategory tc
      where pt.deletedAt is null
        and t.deletedAt is null
        and pt.product.id in ( ? )
      order by pt.product.id, tc.tagCode, t.tagName
```
	
     파일: src/main/java/com/deskit/deskit/product/repository/ProductTagRepository.java
    
  3. 썸네일/갤러리 이미지 조회 (단건)
```sql
     select *
       from ProductImage
      where product_id = ?
        and image_type = 'THUMBNAIL'
        and slot_index = 0
        and deleted_at is null
      order by id asc
     select *
       from ProductImage
        and deleted_at is null
      order by slot_index asc
```
	
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java

  4. 라이브 가격 단건 조회
```sql
       from BroadcastProduct
      where product_id = ?
        and (라이브 상태 조건...)
```
	
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java


### **성능 개선**
###### 챗봇의 벡터 유사도 Threshold 조정
기존: 0.75
변경: 0.7

변경 전에는 "판매자 그룹 정책에 대해 알려줘" 질문에 답변 불가(일반LLM)
변경 후에는 동일 질문에 판매자 정책 문서를 근거로 답변을 줌(RAG)


###### RAG 품질 향상
문제 상황
문서를 근거로 답변을 주는데, 거의 문서 전문을 답변으로 주는 경우가 있음
그리고 어느 문서를 근거로 답변을 주는지 검증이 필요

개선 방향
문서 임베딩시 문서를 여러 개의 청크로 나눠서 저장
청크 크기: 500토큰, 최소 토큰 100개로 설정
청크마다 인덱스 번호를 메겨 RAG 답변 호출시 어느 청크를 근거로 가져왔는지 로그로 확인

---
# 로직 설명
###### VOD 기능
방송 종료 -> VOD 생성 로직은 현재 이렇게 동작합니다.                                                                                                                                                                              
  1. 방송 송출 중 녹화 시작                                                                                                                                                                                                         
  - 프론트에서 퍼블리셔 트랙 준비 후 녹화 시작 API 호출
    front/src/pages/seller/LiveStream.vue:809, front/src/pages/seller/LiveStream.vue:811, front/src/pages/seller/LiveStream.vue:1022                                                                                                
  - API 호출 함수
    front/src/lib/live/api.ts:450                                                                                                                                                                                                   
  - 백엔드 엔드포인트
    src/main/java/com/deskit/deskit/livehost/controller/seller/BroadcastSellerController.java:148                                                                                                                                   
  - 서비스에서 ON_AIR 상태 확인 후 OpenVidu 녹화 시작(+재시도 큐)
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:685
    src/main/java/com/deskit/deskit/livehost/service/OpenViduService.java:116                                                                                                                                                       
  2. 방송 종료 시 녹화 중지

  - 종료 API
    src/main/java/com/deskit/deskit/livehost/controller/seller/BroadcastSellerController.java:157                                                                                                                                   
  - 종료 처리: 상태 ENDED, 시청 이력 마감, stopRecording, 세션 close
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:657
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:674
    src/main/java/com/deskit/deskit/livehost/service/OpenViduService.java:144                                                                                                                                                       
  3. OpenVidu webhook 수신 시 VOD 생성                                                                                                                                                                                              
  - 웹훅 엔드포인트에서 recordingStatusChanged + ready일 때 처리
    src/main/java/com/deskit/deskit/livehost/controller/BroadcastPublicController.java:135
    src/main/java/com/deskit/deskit/livehost/controller/BroadcastPublicController.java:141                                                                                                                                          
  - processVod에서:
  - sessionId에서 broadcastId 파싱
  - 중복 VOD 생성 방지 체크
  - OpenVidu 녹화 mp4를 가져와 S3 업로드(uploadVodWithRetry)
  - vod 테이블 저장 (URL/size/duration/status)
  - 방송 결과 통계(broadcast_result) 저장/갱신
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1031

  4. 웹훅 누락 대비 보정
  - 스케줄러/재시도 큐가 녹화 상태를 폴링해 ready면 강제로 processVod 실행
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1712
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1728
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1750


# 모니터링
Grafana:
ID : admin / PW : 11ehdbs23


# 방송 입장 성능 저하 관련 모니터링 결과
### 원인 진단

• 확인 완료. Grafana API로 직접 같은 구간(2026-03-23 15:56:13 ~ 16:26:13 KST) 조회해보니 병목은 DB 커넥션 풀 포화가 맞습니다.

  핵심 근거:

  - hikaricp_connections_max = 10 고정
  - hikaricp_connections_active 최대 10 (풀 꽉 참)
  - hikaricp_connections_pending 최대 175 (대기열 폭증)
  - hikaricp_connections_timeout_total 30분 증가량 약 19
  - join 평균 지연(서버측) 최대 약 19,447ms
  - CPU는 낮음(process_cpu_usage 최대 0.1 수준)
  - 4xx 없음, 5xx는 소량(30분 약 18건/4407건)

  장애 시점:

  - 대기열 피크: 2026-03-23 16:07:43 KST (pending=175)
  - 앱 up=0 구간: 16:08:13 ~ 16:09:43 KST (짧은 다운/재기동 또는 scrape 단절)

  결론:

  - OpenVidu/CPU보다 먼저, DB 커넥션 풀이 먼저 막히면서 join 요청이 줄서고 timeout/drop이 발생한 패턴입니다.
  - k6의 p95 38s, droppedIterations 694와 일치합니다.

  바로 할 것:

  1. Hikari 풀 상향: maximum-pool-size를 최소 10 -> 30 (DB 허용치 맞춰 조정)
  2. join 경로 DB 왕복 축소: recordViewEnter(조회+삽입) 비동기/배치 또는 Redis 우선 처리
  3. join 관련 인덱스 점검: view_history(broadcast_id, viewer_id, exited_at), sanction(broadcast_id, member_id, status)
  4. 타임아웃/보호 설정: DB timeout 짧게 + join API bulkhead/rate limit
  5. 대시보드 보강: tomcat_threads_busy, hikari_timeout_total, join p95(서버)를 확실히 표시
      - 현재 Join/Leave p95 패널 쿼리는 데이터가 안 나와서(버킷 미수집 가능) 수정 필요

### Attempt 1
• 확인했습니다. 이번 테스트 구간(2026-03-23 09:01:00 ~ 09:11:00 UTC, 18:01:00 ~ 18:11:00 KST) 기준으로 대시보드/Prometheus 수치 같이 보면:

  핵심 결과:

  - k6: p95 34,197.93ms (이전 38,582.69ms 대비 약 11% 개선)
  - k6: droppedIterations 468 (이전 694 대비 개선)
  - 서버 join 평균 지연: avg 3,471ms, max 19,545ms
  - Hikari:
      - max=30까지 꽉 참
      - active max=30
      - pending max=171 (여전히 큼)
      - timeout rate max=0.083/s
  - up=0 구간 존재:
      - 2026-03-23 09:09:45 ~ 09:10:30 UTC (18:09:45 ~ 18:10:30 KST)

  해석:

  - 풀 확장 효과는 분명 있음(완화됨).
  - 하지만 여전히 DB 대기열(pending)이 크게 쌓이고, 중간에 앱 다운/재시작 구간까지 겹쳐 tail latency가 다시 길어집니다.
  - 즉, 지금 병목은 “풀 부족 단일 원인”에서 “DB 처리시간 + 앱 안정성” 단계로 넘어간 상태입니다.

  중요 보충:

  - Join/Leave p95 (Server) 패널은 현재도 데이터가 비어 있습니다(버킷 계열 미수집/라벨 불일치 가능).
    대신 Join/Leave Avg Latency (Server) 패널로 추세는 정상 확인됩니다.

### Attempt 2
• 분석 완료했고, 대시보드도 보강 반영했습니다(version 4).

  이번 런 결론:

  - 개선은 됨:
      - p95Ms: 38,582 -> 34,198 (약 11% 개선)
      - droppedIterations: 694 -> 468 개선
  - 하지만 아직 실패:
      - hikaricp active가 30까지 지속 포화
      - hikaricp pending 최대 171로 대기열 큼
      - hikaricp timeout rate 발생(최대 0.083/s)
      - join 서버 평균 지연 최대 19.5s
  - up=0 구간은 재시작이 아니라 스크레이프 타임아웃 성격:
      - process_uptime_seconds는 계속 증가(리셋 없음)
      - scrape_duration_seconds 최대 5.001s (스크레이프 실패 시점과 일치)

  즉, 현재 상태는:

  - 풀 30으로 올려도 DB 대기열이 계속 쌓임
  - 애플리케이션이 순간적으로 매우 느려져 Prometheus scrape까지 timeout

  대시보드 추가 반영:

  - 기존 Tomcat Threads 패널 -> JVM Threads (Live/Peak)로 교체
  - Prometheus Scrape Duration 신규
  - App Up 신규
  - 기존 보강 패널(Hikari Timeout Rate, Hikari Pool, Join/Leave Avg) 유지

  다음 우선순위:

  1. join 경로 DB 쿼리 프로파일링(슬로우로그 + 쿼리별 평균/최대)
  2. join API에 대해 DB timeout/벌크헤드(동시성 제한) 적용
  3. OpenVidu token 발급 지연 계측(메트릭 추가)으로 DB 병목과 분리

  원하면 내가 다음 턴에서 joinBroadcast에 타이머 계측(토큰 발급/제재조회/전체)을 바로 코드에 심어서, 다음 k6에서 병목 구간을 정확히 보이게 만들어줄게요. 

### Attempt 3
• 종합 결론: 가용성은 유지되지만, 지연 병목은 여전히 심각합니다.

  - 2회 연속 결과가 거의 동일:
      - p95: 42.7s -> 41.0s (소폭 개선)
      - droppedIterations: 876 -> 796 (소폭 개선)
      - joinSuccessRate: 99.7%대 유지
      - join5xx: 11 -> 14 (소수지만 존재)
  - 실패 원인은 명확히 http_req_duration, join_duration_ms만 초과
    -> “대부분 성공하지만 너무 늦게 성공” 패턴

  Grafana까지 합친 해석 포인트

  - 이번 진단 핵심은 신규 패널 4개입니다:
      - Join Stage Avg: openvidu.createToken이 튀면 OpenVidu 병목
      - Join DB Query Avg/Max: sanction.findLatest, member.findByLoginId가 튀면 DB 병목
      - Join Bulkhead: inflight/rejected로 앱 내부 동시성 제한 동작 확인
      - Join Total: total만 크면 큐잉/대기 문제
  - 현재 k6 숫자만 보면, 에러율이 낮아서 하드 실패보다 대기열/큐잉 지연이 주원인입니다.                                                                                                                                             

  1. openvidu.createToken vs DB query 두 축 중 어느 쪽이 join total을 주도하는지 패널 수치로 확정
  2. Join Bulkhead에서 inflight가 한계 근처인데 rejected/s가 거의 0이면, 요청이 앱 밖(커넥터/스레드/네트워크)에서 먼저 대기 중인 구조 점검
  3. join5xx 14건의 서버 로그(Trace-Id) 묶어서 공통 예외 타입 확인 (OPENVIDU_ERROR인지 DB timeout인지)

### Iteration을 20 -> 10으로 조정
= 초당 방문 횟수

• 좋은 판단입니다. 지금 데이터 기준으로는 20 iters/s가 현재 시스템 한계를 넘겨서 큐잉 지연을 만든 상태라, 10 iters/s로 낮춰서 정상 운영 구간을 먼저 보는 게 맞습니다.

  1. join_only를 startRate=3, rateStep=2, stages=4로 실행
  2. join_only를 startRate=3, rateStep=2, stages=4로 실행
     -> 최종 9 iters/s
  3. 이어서 startRate=4, rateStep=2, stages=4
     -> 최종 10 iters/s
  4. 두 런에서 p95, droppedIterations, Join Stage Avg(특히 openvidu.createToken) 비교

  즉, 바로 10으로 내려서 안정 구간 확인하고, 그 다음 12/14로 다시 올리며 한계점을 찾는 방식이 가장 효율적입니다.

**-> 테스트 성공**

# Iteration 20에서 최적화하기
Q. 근데 iter 20일때 보면 JVM 쓰레드랑 Hikari 풀이 터져서 서버도 같이 죽는 것 같은데 그러면 어떻게 최적화를 해야 해?


• 맞습니다. 지금 패턴이면 “성능 저하”가 아니라 **큐잉 붕괴(쓰레드 점유 + DB 커넥션 고갈)**에 가깝습니다.

  핵심은 이거예요:

  - joinBroadcast가 @Transactional이고, 그 안에서 openViduService.createToken()(외부 네트워크 I/O)을 호출 중입니다.
  - OpenVidu가 느려지면 요청 쓰레드가 오래 점유되고, 트랜잭션/DB 커넥션 점유 시간도 길어져 Hikari pending/timeout이 같이 터집니다.
  - 그 상태에서 Tomcat 쓰레드까지 포화되면 scrape timeout, 사실상 “서버 죽은 것처럼” 보입니다.

  최적화 우선순위는 아래 순서가 가장 효과 큽니다.

  1. 트랜잭션 경계 분리 (가장 중요)

  - DB 조회/제재검사까지만 짧은 read-only 트랜잭션
  - createToken은 트랜잭션 밖에서 실행
  - 즉, “DB 커넥션 잡은 채 외부 호출” 구조를 끊어야 합니다.

  1. OpenVidu 호출 격리

  - 전용 bulkhead(세마포어) + 짧은 timeout + 즉시 실패(429/503)
  - 실패 재시도는 요청 경로에서 하지 말고 제한적으로
  - 이미 timeout 넣었지만, 트랜잭션 분리가 먼저입니다.

  1. 서버 보호값(붕괴 방지)

  - Tomcat max-threads, accept-count, connection-timeout 조정해서 무한 대기열 방지
  - Hikari connection-timeout 짧게(예: 1~2s)로 fail-fast
  - 목표는 “느리게 다 받기”가 아니라 “빨리 거절해서 전체 생존”입니다.

  1. DB 쿼리 튜닝

  - sanction.findLatest 인덱스 점검(방송ID+멤버ID+최신순)
  - member.findByLoginId 인덱스/유니크 보장
  - 느린 쿼리 로그 기준으로 실제 상위 쿼리만 정리

  1. 임계부하 재정의

  - 현재는 10 iters/s 안정, 20 iters/s 붕괴
  - 운영 SLO 기준선은 10 근처로 두고, 12/14/16 단계로 상향 테스트

### Iter 20 테스트 재시도 결과
맞아요. 이번 결과는 이전과 거의 같은 패턴이고, 트랜잭션 분리 효과는 제한적입니다.

  비교 요약:

  - p95: ~41.0s -> 38.2s (소폭 개선)
  - dropped: ~796 -> 809 (비슷)
  - 성공률/실패율: 여전히 좋음 (joinSuccessRate 99.64%, 5xx 15건)
  - 결론: 에러가 아니라 대기열 지연 문제 지속

  핵심 해석:

  - join 자체 실패는 거의 없음
  - 그런데 http_req_duration만 크게 폭증
  - 즉, 요청이 “처리 실패”가 아니라 “너무 늦게 처리”됨

  Grafana와 같이 보면 판단 기준:

  1. deskit_join_total_duration_seconds_*가 높다

  - 앱 내부 병목 (OpenVidu/DB/락) 가능성 큼

  1. deskit_join_total은 낮은데 k6 http_req_duration만 높다

  - 앱 진입 전 구간(LB/Nginx/Tomcat accept queue) 대기 병목 가능성 큼

  지금 “거의 그대로”라면, 보통 2번일 확률이 높습니다.
  (트랜잭션 분리했는데도 체감 개선이 거의 없으니까요)

  다음 우선순위:

  1. join_total vs http_server_requests/k6 지연을 같은 구간에서 직접 비교
  2. Tomcat/LB 큐 보호 설정 강화 (max-threads, accept-count, keepalive/connection timeout)
  3. openvidu.createToken 전용 bulkhead/작은 큐/빠른 reject(429 or 503)로 “느린 성공”을 “빠른 거절”로 전환





# 최종 적용
• 적용 완료했습니다. 목표(p95 유지 + 최종 성공률 95%+) 기준으로 1~3번을 반영했습니다.

  변경 사항:

  1. 429로 명시적 제한 응답

  - limit_req_status 429;
  - limit_conn_status 429;
  - Retry-After: 1 헤더 추가

  1. 트래픽 제어 튜닝(과도한 차단 완화)

  - 전역/사용자 이중 제한으로 변경
  - 전역: join_req_global rate=80r/s, join_conn_global 600
  - 사용자: join_req_per_user rate=20r/s, join_conn_per_user 30
  - 사용자 키는 X-Viewer-Id 우선, 없으면 IP fallback

  1. 프론트 join 재시도(429 대응)

  - joinBroadcast에 자동 재시도 추가
  - 최대 5회, 지수 백오프 + 지터
  - Retry-After 헤더가 있으면 우선 적용

  수정 파일:

  - 02-nginx-join-backpressure.conf.template
  - default
  - api.ts

  다음 적용 순서:

  1. systemctl reload nginx
  2. 프론트 배포(재시도 로직 반영)
  3. k6 재측정 (20 iters/s) 후 p95 / joinSuccessRate / 429 비율 확인


# 패널 지표 4개 수정, 4xx 응답으로 유도
지금 결과는 아주 의미 있습니다. 상태가 “서버 붕괴”에서 “제어 가능한 과부하”로 바뀌었습니다.

  핵심 해석:

  - 개선됨:
      - p95 38~42s -> 2.4s
      - droppedIterations 0
      - join5xx 0 (백엔드 장애성 실패 사라짐)
  - 남은 문제:
      - join4xx 988 / joinSuccessRate 80.6%
      - 임계치 실패 원인: http_req_failed, join_success_rate, p95
  - Grafana 상 병목:
      - Join Stage Avg: openvidu.createToken이 2s+로 급상승
      - Join DB Query Avg/Max: 수 ms~수십 ms 수준 (DB는 주병목 아님)
      - Join Bulkhead: inflight가 상한 근처, rejected/s 증가
      - => 현재 실패의 대부분은 의도된 제한(429) + OpenVidu 단계 지연

  다음 튜닝(목표: p95 유지 + 성공률 95%+):

  1. Nginx 완화

  - join_req_per_user rate 20r/s -> 30r/s
  - burst 20 -> 40
  - join_conn_per_user 30 -> 50
    (글로벌 제한은 유지)

  1. 앱 bulkhead 소폭 완화

  - deskit.join.bulkhead.max-concurrent 25 -> 30

  1. 프론트 재시도 강화

  - 시도 5 -> 7, 최대 backoff 2.5s -> 4s

  1. 재테스트(20 iters/s) 후 기준

  - joinSuccessRate >= 0.95
  - join4xx(429) 비율 크게 감소
  - p95 1.5~2.0s 범위 유지

-> 결론: OpenVidu 토큰 단계가 느리다, 다른거 튜닝해도 성능 비슷
      - OpenVidu 토큰 발급 경로 분리(전용 워커/큐) 또는 OpenVidu 용량 증설
      - join 요청 선승인 후 비동기 토큰 전략(UX 재시도와 결합)

# OpenVidu 토큰 발급 지연 해결
• 두 가지 방법입니다.

  1. 토큰 발급 경로 분리(코드/구조)

  - 핵심: join API에서 OpenVidu 호출을 직접 오래 붙잡지 않게 분리

  빠른 방식(현실적):

  1. join을 2단계로 분리

  - POST /join/request
    DB 검증 + 입장 슬롯 확인만 하고 requestId 반환
  - GET /join/token?requestId=...
    토큰 준비되면 반환, 아니면 202(pending) + 재시도

  1. OpenVidu 토큰 발급은 전용 실행기에서 처리

  - 별도 ThreadPool(작은 큐 + 제한된 스레드)
  - timeout/벌크헤드 강하게 적용

  1. 프론트는 pending polling + 백오프

  - 사용자 체감은 “입장 중”으로 유지, 서버는 블로킹 감소

  장점:

  - 앱 메인 요청 스레드/DB 커넥션 점유 감소
  - 피크 시 graceful degradation 가능

  1. OpenVidu 용량 증설(인프라)

  - 핵심: createToken 지연 자체를 줄임

  실행 항목:

  1. OpenVidu 서버 리소스 상향

  - vCPU/메모리 증설 (CPU 여유 확보)

  1. OpenVidu 수평 확장

  - OpenVidu Pro/Enterprise(클러스터) 또는 미디어 노드 분리

  1. 네트워크 지연 최소화

  - 앱 서버와 OpenVidu를 같은 리전/가까운 AZ 배치

  1. OpenVidu 측 모니터링 확인

  - API latency, CPU, connection/session 수, GC, network saturation

  어떤 걸 먼저?

  - 단기: 인프라 증설(가장 빠른 효과)
  - 중기: 토큰 발급 경로 분리(근본 안정성)

# OpenVidu 서버 업스케일
• 좋습니다. 이번 결과는 사실상 목표 달성입니다.

  요약:

  - p95Ms 145.52 (목표 1500ms 충분히 통과)
  - httpReqFailedRate 0, joinSuccessRate 1.0
  Grafana와 일치:

  - Join Stage Avg에서 openvidu.createToken이 약 90~120ms 수준으로 안정
  - Join Total avg/max도 100~200ms대
  - Join Bulkhead 거부 거의 없음
  - DB query는 수 ms~수십 ms로 병목 아님

  결론:

  - 병목은 앱/DB가 아니라 OpenVidu 용량 부족이 핵심이었고,
  - t3.large -> t3.xlarge 업스케일로 20 iters/s 구간이 안정화됨.

  다음 권장:

  1. 같은 조건으로 2~3회 재측정(재현성 확인)
  2. 25/30 iters/s 단계 테스트로 새 한계점 파악
  3. 운영 기준선: 현재 20 iters/s는 안전 구간으로 업데이트


