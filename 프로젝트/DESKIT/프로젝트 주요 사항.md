
---
# 주요 문제 해결
### **JPA N+1 문제**
요약하면, 이 프로젝트는 상품 목록 조회에서 연관 엔티티를 개별로 지연로딩하지 않고, 
필요한 데이터만 “배치 조회 + DTO 조합”으로 N+1을 피합니다. 
그리고 전역적으로 Hibernate batch fetch size도 설정되어 있습니다.

핵심 대응 방식 (상품 조회 기준)

  1. 상품 목록 조회 시 태그를 한 번에 배치 조회
     `ProductService.getProducts()`에서 상품 목록을 먼저 가져온 다음,  상품 ID 리스트로 태그를 IN 쿼리로 한 번에 조회합니다.                                                                                                           
     이때 `ProductTagRepository.findActiveTagsByProductIds(...)`는 projection 쿼리로 필요한 컬럼만 조회해 DTO로 조합합니다.                                                                                                           
     파일: `src/main/java/com/deskit/deskit/product/service/ProductService.java`
     파일: `src/main/java/com/deskit/deskit/product/repository/ProductTagRepository.java`                                                                                                                                            
  2. 썸네일/이미지, 라이브 가격도 배치 조회
     같은 `getProducts()`에서 이미지와 라이브 가격도 productIds로 한 번에 조회하고, 결과를 맵으로 묶어 응답 DTO에 조합합니다.
	  
  3. Hibernate batch fetch size 설정
     전역적으로 `hibernate.default_batch_fetch_size=100`이 설정되어 있어, LAZY 연관 로딩이 발생하더라도 묶음으로 가져오는 힌트가 제공됩니다.
     파일: `src/main/resources/application.properties`


아래는 상품 목록 조회(getProducts) 기준으로 실제 나가는 쿼리 흐름을 단계별로 정리한 것입니다. 
단건 조회(getProduct)는 별도 섹션에 덧붙입니다.

###### 상품 목록 조회 흐름 (ProductService.getProducts)

  1. 상품 기본 목록 조회
      - 목적: 삭제되지 않았고 판매중인 상품만 가져옴
      - 쿼리 형태:
```sql
		select *
		from Product
		where status = 'ON_SALE'
		order by id asc
```
        
      - 파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
      
  2. 썸네일 이미지 일괄 조회
      - 목적: 상품별 썸네일(슬롯 0)만 한 번에 조회
      - 쿼리 형태:
```sql
        select *
        from ProductImage
        where product_id in ( ...productIds... )
            and image_type = 'THUMBNAIL'
            and slot_index = 0
        order by product_id asc, id asc
```
		
      - 파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
      
  3. 라이브 가격 일괄 조회
      - 목적: 방송 중 상품의 현재 가격 오버라이드 조회
      - 쿼리 형태(개념적으로):
```sql
        select product_id, bp_price
        from BroadcastProduct
        and (라이브 상태 조건...)
```
		
      - 파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
    
  4. 상품 태그 일괄 조회 (N+1 방지 핵심)
      - 목적: 상품별 태그를 한 번에 가져와 DTO로 조립
      - 쿼리 형태:
```sql
	    select pt.product.id as productId,
               tc.tagCode as tagCode,
               t.tagName as tagName
        from ProductTag pt
        join pt.tag t
        join t.tagCategory tc
        where pt.deletedAt is null
           and t.deletedAt is null
           and tc.deletedAt is null
           and pt.product.id in ( ...productIds... )
        order by pt.product.id, tc.tagCode, t.tagName
```

  5. 메모리에서 DTO 조합
      - 쿼리 없음 (메모리 처리)
  
###### 상품 단건 조회 흐름 (ProductService.getProduct)

  1. 상품 단건 조회
```sql
     select *
       from Product
        and status = 'ON_SALE'
        and deleted_at is null
```
	
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java

  2. 태그 일괄 조회 (단건도 동일 로직 재사용)
```sql
     select pt.product.id as productId,
            tc.tagCode as tagCode,
            t.tagName as tagName
       from ProductTag pt
       join pt.tag t
       join t.tagCategory tc
      where pt.deletedAt is null
        and t.deletedAt is null
        and pt.product.id in ( ? )
      order by pt.product.id, tc.tagCode, t.tagName
```
	
     파일: src/main/java/com/deskit/deskit/product/repository/ProductTagRepository.java
    
  3. 썸네일/갤러리 이미지 조회 (단건)
```sql
     select *
       from ProductImage
      where product_id = ?
        and image_type = 'THUMBNAIL'
        and slot_index = 0
        and deleted_at is null
      order by id asc
     select *
       from ProductImage
        and deleted_at is null
      order by slot_index asc
```
	
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java

  4. 라이브 가격 단건 조회
```sql
       from BroadcastProduct
      where product_id = ?
        and (라이브 상태 조건...)
```
	
     파일: src/main/java/com/deskit/deskit/product/service/ProductService.java


### **성능 개선**
###### 챗봇의 벡터 유사도 Threshold 조정
기존: 0.75
변경: 0.7

변경 전에는 "판매자 그룹 정책에 대해 알려줘" 질문에 답변 불가(일반LLM)
변경 후에는 동일 질문에 판매자 정책 문서를 근거로 답변을 줌(RAG)


###### RAG 품질 향상
문제 상황
문서를 근거로 답변을 주는데, 거의 문서 전문을 답변으로 주는 경우가 있음
그리고 어느 문서를 근거로 답변을 주는지 검증이 필요

개선 방향
문서 임베딩시 문서를 여러 개의 청크로 나눠서 저장
청크 크기: 500토큰, 최소 토큰 100개로 설정
청크마다 인덱스 번호를 메겨 RAG 답변 호출시 어느 청크를 근거로 가져왔는지 로그로 확인

---
# 로직 설명
###### VOD 기능
방송 종료 -> VOD 생성 로직은 현재 이렇게 동작합니다.                                                                                                                                                                              
  1. 방송 송출 중 녹화 시작                                                                                                                                                                                                         
  - 프론트에서 퍼블리셔 트랙 준비 후 녹화 시작 API 호출
    front/src/pages/seller/LiveStream.vue:809, front/src/pages/seller/LiveStream.vue:811, front/src/pages/seller/LiveStream.vue:1022                                                                                                
  - API 호출 함수
    front/src/lib/live/api.ts:450                                                                                                                                                                                                   
  - 백엔드 엔드포인트
    src/main/java/com/deskit/deskit/livehost/controller/seller/BroadcastSellerController.java:148                                                                                                                                   
  - 서비스에서 ON_AIR 상태 확인 후 OpenVidu 녹화 시작(+재시도 큐)
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:685
    src/main/java/com/deskit/deskit/livehost/service/OpenViduService.java:116                                                                                                                                                       
  2. 방송 종료 시 녹화 중지

  - 종료 API
    src/main/java/com/deskit/deskit/livehost/controller/seller/BroadcastSellerController.java:157                                                                                                                                   
  - 종료 처리: 상태 ENDED, 시청 이력 마감, stopRecording, 세션 close
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:657
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:674
    src/main/java/com/deskit/deskit/livehost/service/OpenViduService.java:144                                                                                                                                                       
  3. OpenVidu webhook 수신 시 VOD 생성                                                                                                                                                                                              
  - 웹훅 엔드포인트에서 recordingStatusChanged + ready일 때 처리
    src/main/java/com/deskit/deskit/livehost/controller/BroadcastPublicController.java:135
    src/main/java/com/deskit/deskit/livehost/controller/BroadcastPublicController.java:141                                                                                                                                          
  - processVod에서:
  - sessionId에서 broadcastId 파싱
  - 중복 VOD 생성 방지 체크
  - OpenVidu 녹화 mp4를 가져와 S3 업로드(uploadVodWithRetry)
  - vod 테이블 저장 (URL/size/duration/status)
  - 방송 결과 통계(broadcast_result) 저장/갱신
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1031

  4. 웹훅 누락 대비 보정
  - 스케줄러/재시도 큐가 녹화 상태를 폴링해 ready면 강제로 processVod 실행
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1712
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1728
    src/main/java/com/deskit/deskit/livehost/service/BroadcastService.java:1750

